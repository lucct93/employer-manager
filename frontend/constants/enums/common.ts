export enum TimeDebounce {
	TIME = 500,
}
export enum Pagination {
	PAGE = 1,
	LIMIT = 100,
	LIMIT_MAX = 1000,
	LIMIT_TABLE = 6,
}
export enum Status {
	SUCCESS = 200,
	SUCCESS_ADD = 201,
}
export enum Breakpoint {
	SDesktop = 1280,
	SmDesktop = 1024,
	SmTablet = 768,
	LDPhone = 576,
	MDPhone = 475,
}
export enum NUMBER {
	MIN = 1,
	MAX = 1000000000000,
}
export enum GENDER {
	MALE = 'M',
	FEMALE = 'F',
}
